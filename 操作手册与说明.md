# 肥嘟嘟桌宠操作手册与说明

本手册面向希望直接安装、切换、维护或二次开发肥嘟嘟桌宠的用户。项目目录名为 `HatchPet-Feidudu-main`，桌宠内部 ID 为 `feidudu`，显示名称为“肥嘟嘟”。

## 1. 你将得到什么

正式安装包位于 `pet/`：

- `pet/pet.json`：桌宠名称、ID、版本和图集路径；
- `pet/spritesheet.webp`：20 帧动态 Sprite V2 图集；
- `assets/spritesheet-static.webp`：减少动态效果时使用的静态备用图集。

肥嘟嘟会根据客户端状态切换待机、左右移动、打招呼、跳跃、受阻、等待输入、工作和等待审阅等动作；鼠标指针移动时，还可以使用 16 个方向的注视姿态。

## 2. 安装前准备

1. 确认已经安装支持自定义桌宠的 ChatGPT/Codex 客户端。
2. 安装 Python 3.10 或更高版本。
3. 解压或复制完整的 `HatchPet-Feidudu-main` 目录，不要只复制 `scripts/`。
4. 打开 PowerShell、终端或命令提示符，进入项目根目录。

Windows PowerShell 示例：

```powershell
Set-Location "D:\ChatGPT-Pets\HatchPet-Feidudu-main"
python --version
```

macOS/Linux 示例：

```bash
cd /path/to/HatchPet-Feidudu-main
python3 --version
```

正常安装不需要安装 Pillow；只有重建图片或运行图像质检时才需要：

```bash
python -m pip install -r requirements.txt
```

## 3. 推荐安装方式

Windows：

```powershell
python scripts\install.py
```

macOS/Linux：

```bash
python3 scripts/install.py
```

安装器会先检查 `pet.json` 和图集是否存在；如发现旧版肥嘟嘟，会按 UTC 时间戳备份到 `~/.codex/backups/feidudu/`，再复制新版本。默认还会更新 `~/.codex/config.toml`：

```toml
[desktop]
selected-avatar-id = "custom:feidudu"
```

看到 `Installed 肥嘟嘟 to ...` 后，完全退出并重新打开客户端。仅关闭一个窗口有时不会终止后台进程，因此建议从系统托盘退出后再启动。

## 4. 在客户端中启用与操作

1. 打开 **Settings → Pets（设置 → Pets）**。
2. 如果列表没有更新，点击 **Refresh** 或重新启动客户端。
3. 选择 **肥嘟嘟**。
4. 使用客户端提供的 **Wake Pet** 或 `/pet` 唤出桌宠。
5. 使用 **Tuck Away Pet** 收起桌宠。
6. 拖动桌宠时会触发左右跑步；任务状态变化由客户端自动映射到对应动画。

客户端会决定桌宠在屏幕上的位置以及何时切换状态，本项目只提供身份清单和图集，不会读取键盘、文件或网络数据。

## 5. 只安装、不立即切换

如果希望保留当前桌宠选择：

```bash
python scripts/install.py --no-select
```

之后可在 **Settings → Pets** 中手动选择肥嘟嘟。

## 6. 安全测试安装

想先验证安装器输出、又不修改真实 `~/.codex` 时，在项目根目录运行：

```bash
python scripts/install.py --codex-home .tmp-codex
```

应生成：

```text
.tmp-codex/
├── config.toml
└── pets/
    └── feidudu/
        ├── pet.json
        └── spritesheet.webp
```

这个 `.tmp-codex` 只是测试目录，可以在确认后手动删除。

## 7. 手动安装

当 Python 安装器不可用时，可手动完成相同操作。

Windows PowerShell：

```powershell
$target = Join-Path $HOME ".codex\pets\feidudu"
New-Item -ItemType Directory -Force $target
Copy-Item pet\pet.json $target -Force
Copy-Item pet\spritesheet.webp $target -Force
```

macOS/Linux：

```bash
mkdir -p ~/.codex/pets/feidudu
cp pet/pet.json pet/spritesheet.webp ~/.codex/pets/feidudu/
```

如需自动选中，请在 `~/.codex/config.toml` 的 `[desktop]` 段中加入或更新：

```toml
selected-avatar-id = "custom:feidudu"
```

如果已有 `[desktop]` 段，不要创建第二个同名段，只需修改其中的键。

## 8. 动态效果与静态模式

动态图集由 20 个全局相位组成，每帧 80ms，总循环 1.60 秒。所有状态共享同一图像时钟，因此无论切入哪一相位都能继续循环；左右跑步各在 1.60 秒内完成两轮 0.80 秒步态。

如果你对持续动画敏感，或希望降低动态效果，可把静态图集覆盖到安装目录。

Windows PowerShell：

```powershell
Copy-Item assets\spritesheet-static.webp "$HOME\.codex\pets\feidudu\spritesheet.webp" -Force
```

macOS/Linux：

```bash
cp assets/spritesheet-static.webp ~/.codex/pets/feidudu/spritesheet.webp
```

重启客户端后生效。要恢复动态版本，再运行一次 `scripts/install.py`。

## 9. 更新与回退

更新时直接在新版项目目录重新运行安装器。安装器会自动保存旧安装：

```text
~/.codex/backups/feidudu/YYYYMMDDTHHMMSSZ/
```

需要回退时：

1. 完全退出客户端；
2. 找到想恢复的时间戳目录；
3. 把其中的 `pet.json` 和 `spritesheet.webp` 复制回 `~/.codex/pets/feidudu/`；
4. 重新启动客户端。

## 10. 卸载

卸载前请确认目标目录确实是 `~/.codex/pets/feidudu/`，不要删除整个 `~/.codex` 目录。

1. 完全退出客户端；
2. 删除 `~/.codex/pets/feidudu/`；
3. 打开 `~/.codex/config.toml`，删除或修改 `selected-avatar-id = "custom:feidudu"`；
4. 重新启动客户端并选择其他桌宠。

如希望保留恢复能力，可先把 `feidudu` 目录移动到个人备份位置，而不是直接删除。

## 11. 状态与画面说明

| 状态 | 触发语义 | 画面说明 |
|---|---|---|
| `idle` | 空闲 | 呼吸、眨眼、轻微口型与回稳 |
| `running-right` | 向右拖动/移动 | 面朝屏幕右侧跑动 |
| `running-left` | 向左拖动/移动 | 面朝屏幕左侧跑动 |
| `waving` | 唤醒或问候 | 单手挥动、开心吐舌 |
| `jumping` | 积极事件 | 蓄力后跳起并回落 |
| `failed` | 任务失败或受阻 | 沮丧、耳朵与身体下沉 |
| `waiting` | 等待用户输入 | 举手或靠近胸前请求注意 |
| `running` | Codex 工作中 | 在橙色笔记本电脑前输入与阅读 |
| `review` | 成果等待检查 | 使用同一台电脑停顿、眨眼、确认 |
| gaze rows | 指针方向 | 16 个方位按 22.5° 间隔排列 |

角色固定特征为：金黄色梨形胖身体、两只长耳、奶油色肚皮、红棕色大鼻子、圆眼睛、两只手、两只脚和一条弯曲尾巴。默认不穿衣服。工作和审阅状态使用橙色笔记本电脑；爱心只属于特殊表情道具。

## 12. 常见问题排查

### Pets 列表里没有肥嘟嘟

- 确认 `~/.codex/pets/feidudu/pet.json` 和 `spritesheet.webp` 同时存在。
- 打开 `pet.json`，确认 `id` 是 `feidudu`、`spriteVersionNumber` 是 `2`。
- 完全退出客户端，而不是只关窗口，然后重新启动。
- 在 Pets 页面点击 Refresh。
- 确认运行安装器的系统账户与启动客户端的账户相同。

### 显示透明方框、裁切或错行

- 检查安装的是否为本项目的 `pet/spritesheet.webp`。
- 文件尺寸应为 1536 × 2288，单元格应为 192 × 208。
- 不要对图集进行自动旋转、缩放或转码。
- 重新运行安装器覆盖损坏的文件。

### 动画不动

- 检查是否曾把 `assets/spritesheet-static.webp` 覆盖到安装目录。
- 再次运行安装器可恢复动态 WebP。
- 某些客户端或无障碍设置可能主动减少动画，这是正常行为。

### 角色没有看向鼠标

- 指针注视能力由客户端实现；项目已经提供两行共 16 个方位。
- 先确认其他 Sprite V2 桌宠是否能跟随指针，以区分客户端能力和素材问题。

### 安装器提示清单不匹配

- 不要修改 `pet/pet.json` 中的 `id`、`spriteVersionNumber` 或 `spritesheetPath`。
- 从完整项目根目录执行脚本，不要把脚本单独复制到别处运行。

## 13. 开发者重建

安装 Pillow：

```bash
python -m pip install -r requirements.txt
```

从正式图集重新生成帧库和文档图：

```bash
python scripts/build_gallery.py
python scripts/build_readme_assets.py
```

静态图集质检：

```bash
python hatch-pet/scripts/validate_atlas.py assets/spritesheet-static.webp --require-v2
```

动态图集质检：

```bash
python hatch-pet/scripts/validate_atlas.py pet/spritesheet.webp --require-v2 --allow-animated --allow-transparent-rgb-residue
python hatch-pet/scripts/validate_smooth_state_webp.py pet/spritesheet.webp --source-atlas assets/spritesheet-static.webp --phase-manifest assets/state-phases.json --require-all-states --min-motion-clips 12 --max-motion-clips 15
```

通用脚本测试：

```bash
python -m unittest discover -s hatch-pet/tests -v
```

生成资产的关键中间文件包括：

- `assets/source/rows/`：各状态的色键源行；
- `assets/canonical-base.png`：透明角色基准图；
- `assets/frames/`：清理后的独立状态帧；
- `assets/state-phases.json`：20 相位精确映射；
- `assets/validation-*.json`：静态、动态和相位一致性报告。

修改角色后必须同时检查解剖数量、朝向、脚底基线、透明边缘、循环接缝、笔记本电脑一致性以及 16 个注视方向。

## 14. 参考图、隐私与授权

`references/source-images/` 内保留了用户提供的九张参考图及一张联系表，仅用于追溯角色设计。参考图可能带有第三方平台水印或作者标记；生成的桌宠图集中未保留这些标记。

在公开发布、商用或向第三方再分发之前，请自行确认：

- 你拥有参考图和衍生形象所需的使用权；
- 分发范围符合原作者和平台的许可要求；
- 是否应从发行包中排除 `references/source-images/`；
- 项目代码许可与美术素材权利需要分别处理。

## 15. 文件核对清单

交付或安装前建议确认：

- [ ] 项目根目录名为 `HatchPet-Feidudu-main`；
- [ ] `pet/pet.json` 的 ID 为 `feidudu`；
- [ ] `pet/spritesheet.webp` 是 20 帧、1536 × 2288 动态 WebP；
- [ ] `assets/spritesheet-static.webp` 是 RGBA 静态 WebP；
- [ ] 英文 `README.md`、中文 `README.zh-CN.md` 与本手册均存在；
- [ ] 静态与动态验证报告中的 `ok` 均为 `true`；
- [ ] README 展示图只包含肥嘟嘟；
- [ ] 发布前已处理参考图的授权与分发范围。
